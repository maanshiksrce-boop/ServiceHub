# ServiceHub

Phase 1 frontend for a Spring Boot service booking website.

## Run locally

Requirements: Java 17+ and Maven 3.9+.

```bash
mvn spring-boot:run
```

Open http://localhost:8080 in a browser. All pages currently use static demo content; database, authentication, and booking persistence are intentionally not implemented.
